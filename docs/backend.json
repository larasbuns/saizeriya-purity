{
  "entities": {
    "SaizeriyaOutlet": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SaizeriyaOutlet",
      "type": "object",
      "description": "Represents a Saizeriya outlet in Singapore.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Saizeriya outlet."
        },
        "name": {
          "type": "string",
          "description": "The name of the Saizeriya outlet."
        },
        "address": {
          "type": "string",
          "description": "The address of the Saizeriya outlet."
        }
      },
      "required": ["id", "name", "address"]
    },
    "QuizResponse": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "QuizResponse",
      "type": "object",
      "description": "Represents a user's response to a Saizeriya location quiz.",
      "properties": {
        "userName": {
          "type": "string",
          "description": "The name of the user who submitted the quiz."
        },
        "selectedOutletIds": {
          "type": "array",
          "description": "Array of strings representing the names of the selected outlets.",
          "items": {
            "type": "string"
          }
        },
        "submissionDate": {
          "type": "string",
          "description": "The date and time the quiz was submitted.",
          "format": "date-time"
        },
        "score": {
          "type": "number",
          "description": "The number of outlets selected by the user."
        }
      },
      "required": [
        "userName",
        "selectedOutletIds",
        "submissionDate",
        "score"
      ]
    }
  },
  "auth": {
    "providers": ["anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/saizeriyaOutlets/{saizeriyaOutletId}",
        "definition": {
          "entityName": "SaizeriyaOutlet",
          "schema": {
            "$ref": "#/backend/entities/SaizeriyaOutlet"
          },
          "description": "Stores information about Saizeriya outlets.",
          "params": [
            {
              "name": "saizeriyaOutletId",
              "description": "The unique identifier for the Saizeriya outlet."
            }
          ]
        }
      },
      {
        "path": "/quizResponses/{quizResponseId}",
        "definition": {
          "entityName": "QuizResponse",
          "schema": {
            "$ref": "#/backend/entities/QuizResponse"
          },
          "description": "Stores quiz responses submitted by users.",
          "params": [
            {
              "name": "quizResponseId",
              "description": "The unique identifier for the quiz response."
            }
          ]
        }
      }
    ]
  }
}
